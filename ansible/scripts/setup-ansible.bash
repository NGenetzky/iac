#!/bin/bash
# Opt 1: Use pipx
# Opt 2: https://www.buildahomelab.com/2022/04/26/how-to-install-ansible-with-pipenv-pyenv/

# TODO: Do I need *both*?
# pipx install ansible
# pipx install ansible-core
#   installed package ansible-core 2.16.3, installed using Python 3.10.12
#   These apps are now globally available
#     - ansible
#     - ansible-config
#     - ansible-connection
#     - ansible-console
#     - ansible-doc
#     - ansible-galaxy
#     - ansible-inventory
#     - ansible-playbook
#     - ansible-pull
#     - ansible-test
#     - ansible-vault
